//פונקציה שמחזירה לי את המילה מהדאטה שבשרת

export async function getCorrectWord(): Promise<string> {
    return Promise.resolve('pinky');
    // const correctWord =await fetch( `http://localhost:3333`).then((response) =>
    // response.json()
    // );
    // return correctWord;
}

